postgis:
  build: postgis
  hostname: postgis
  volumes:
  - ./postgres_data:/var/lib/postgresql
geoserver:
  build: geoserver
  hostname: geoserver
  links:
  - postgis:postgis
  volumes:
  - ./geoserver_data:/opt/geoserver/data_dir
